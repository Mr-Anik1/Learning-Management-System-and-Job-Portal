// All news letter controllers have been imported
const { subscribe } = require("./subscribe");
const { unsubscribe } = require("./unsubscribe");
const { findAll } = require("./findAll");

// All news letter controllers have been exported
module.exports = { subscribe, unsubscribe, findAll };
